#include<stdio.h>
int main(){
  int num, l, key, i, j;
  printf("How many numbers: ");
  scanf("%d", &num);
  int arr[num];
  printf("Enter the numbers to be sorted: ");
  for(l=0; l<num; l++){
    scanf("%d", &arr[l]);
  }
  for(l=0; l<num; l++){
    p
  }

/*
  for(j=1; j<num; j++){
    key = arr[j];
    i = j-1;
    while(i>0 && arr[i]>key){
      arr[i+1] = arr[i];
      arr[i] = key;
      i--;
    }
  }

  for(l=0; l<num; l++){
    printf("%d\n", arr[l]);
  }
*/
}
